CREATE TABLE NAME
(
NAME CHAR(10)
);

INSERT INTO NAME VALUES ('RAHUL');
INSERT INTO NAME VALUES ('RAHUL');
INSERT INTO NAME VALUES ('RAM');
INSERT INTO NAME VALUES ('VINAY');
INSERT INTO NAME VALUES ('KIRAN');
INSERT INTO NAME VALUES ('RAM');
INSERT INTO NAME VALUES ('RAM');

WITH NBR AS
(SELECT NAME,ROW_NUMBER() OVER (PARTITION BY NAME ORDER BY NAME) NUMBR FROM NAME ORDER BY NAME)
SELECT NAME,CASE
                WHEN NUMBR =1
                    THEN NAME 
                ELSE TRIM(NAME)||(NUMBR-1) END ALIAS FROM NBR ;