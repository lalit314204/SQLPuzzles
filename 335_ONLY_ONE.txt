CREATE TABLE ONE
(
ID CHAR(1),
NUM INT,
VAL CHAR(4)
);

INSERT INTO ONE VALUES ('A',141,'ONE');
INSERT INTO ONE VALUES ('A',141,'TWO');
INSERT INTO ONE VALUES ('A',234,'TWO');
INSERT INTO ONE VALUES ('B',981,'ONE');
INSERT INTO ONE VALUES ('C',121,'ONE');
INSERT INTO ONE VALUES ('C',111,'ONE');

SELECT ID,NUM,VAL FROM ONE T WHERE T.VAL='ONE'
AND NOT EXISTS (
SELECT NULL FROM ONE T1 WHERE T1.ID=T.ID AND T1.VAL<>T.VAL);